<p id="notice"><%= notice %></p>

<h1>People</h1>

<ta-belle id="tabelle" search-src="/" change-uri>
  <table>
    <thead>
      <tr>
        <th name="name" value="<%= @filter[:name] %>" nosort>Name</th>
        <th name="phone" value="<%= @filter[:phone] %>" nosort>Phone</th>
        <th name="email" value="<%= @filter[:email] %>" nosort>Email</th>
        <th name="address" value="<%= @filter[:address] %>" nosort>Address</th>
      </tr>
    </thead>

    <tbody>
      <% @people.each do |person| %>
        <tr>
          <td><%= person.name %></td>
          <td><%= person.phone %></td>
          <td><%= person.email %></td>
          <td>
            <div><%= person.street %></div>
            <div><%= person.city %>, <%= person.zip %></div>
            <div><%= person.country %></div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</ta-belle>

<br>

<%# link_to 'New Person', new_person_path %>
